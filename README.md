# lab07
# git clone https://github.com/kunyikan/lab07.git
# cd lab07
# git add .
# git commit -m "สร้างไฟล์"
# git add .
# git commit -m "การใช้งาน Event onclick และ Alert พื้นฐาน"
# git add .
# git commit -m "การรับค่าจาก input และแสดงผล" 
# git add .
# git commit -m "การใช้ตัวแปรและการคำนวณ"
# git add .
# git commit -m "การใช้ Arrays และ Loops"
# git add .
# git commit -m "การจัดการ DOM (Document Object Model)"
# git add .
# git commit -m "การใช้ Event Listeners"
# git add .
# git commit -m "การใช้ Objects"
# git add .
# git commit -m "การใช้ Timers"
# git add .
# git commit -m "การใช้ Fetch API"
# git add .
# git commit -m "การสร้างเกมง่ายๆ"
# git add .
# git commit -m "ปุ่มเปลี่ยนสี - แสดงการจัดการ events และการสุ่มค่าสี"
# git add .
# git commit -m "ตัวนับจํานวนคลิก - การจัดการตัวแปรและการอัพเดท DOM"
# git add .
# git commit -m "รายการซื้อของ - การจัดการ input และการสร้าง elements"
# git add .
# git commit -m "นาฬิกาดิจิตอล - การใช้ setInterval และการจัดการเวลา"
# git add .
# git commit -m "เครื่องคิดเลขพื้นฐาน - การคํานวณและการจัดการ form"
# git add .
# git commit -m "สไลด์โชว์รูปภาพ - การจัดการ array และการเปลี่ยนรูปภาพ"
# git add .
# git commit -m "ฟอร์มตรวจสอบความถูกต้อง - การ validate form และการจัดการ errors"
# git add .
# git commit -m "ค้นหาและกรองข้อมูล - การกรองข้อมูลแบบ real-time"
# git add .
# git commit -m "แท็บเนื้อหา - การสร้าง tab interface และการจัดการ CSS classes"
# git add .
# git commit -m "รายการสิ่งที่ต้องทํา - การใช้ Local Storage และการจัดการข้อมูลแบบซับซ้อน"
# git add .
# git commit -m "ใส่ code ใน readme"
# git push